# AI 协作记录与提示词归档

> **这份文档为什么存在**
>
> 本作业从第一行代码到最后一笔提交，全程由「人 + AI」协作完成。考核明确要求仓库里保留
> **与 AI 的对话、主要提示词、项目代码、技术及设计文档**四类材料。代码与文档分别见
> `firmware/`、`server/` 与同目录下的几份设计/复盘文档；**这份文件专门归档前三类里的
> 「对话」与「提示词」**，并且如实记录 AI 在哪里帮上了忙、在哪里帮了倒忙。
>
> 归档原则：
> 1. **只记真实发生过的**。凡是原文保留的，标【原文】；凭上下文复述的，标【转述】并说明。
> 2. **不美化**。AI 犯的错、给出的错误结论、被人工推翻的方案，一律写进来 ——
>    这份记录的价值恰恰在于「人怎么判断 AI 说得对不对」，而不是「AI 有多神」。
> 3. **每条提示词都追到产出物**（提交哈希 / 文件路径 / 日志），可逐条复核。

---

## 一、协作模式：谁做什么

### 1.1 角色分工

| 角色 | 承担的事 | 不承担的事 |
|---|---|---|
| **人（我）** | 提出目标与约束；提供硬件、网络、串口、截图等 AI 拿不到的一手信息；**对每个结论做最终裁决**；执行需要物理接触或凭据的操作（插板子、烧录、`git push` 认证） | — |
| **AI** | 读代码/翻日志/查文档；写实现；设计验证方案；**主动指出方案里的漏洞**；把踩过的坑沉淀成文档与技能 | 不接触真实硬件；不代替人做「这个数值物理上是否成立」的判断；不代替人执行对外可见的动作（推送、发消息） |

贯穿全程的一条工作约定（对 AI 的要求）：

> **结论先行，证据跟上。** 不允许出现「应该可以了」「大概是网络问题」这类没有证据的结论；
> 每一个判断都要落到**可复核的东西**上 —— 一段日志、一行数据库记录、一个 HTTP 状态码、
> 一条 git 提交。

### 1.2 一个真实例子：这条约定是怎么起作用的

第 1 周曾出现「板子串口一路报上传失败，服务端日志却明明有 201」的矛盾。如果按直觉走，
结论会是「网络不稳」或者「代码有 bug」，两个方向都错。实际的做法是**先把两边的原始数据
摆在一起比对**：

- 服务端：`readings` 表按 `ts_server` 排序，看条数与时间间隔；
- 板子：串口日志里的失败次数与时间点；
- 结论：服务端确实收到了，但**收到的那几条和板子认为成功的那几条对不上**，
  说明丢包发生在「板子发出 → 服务端收到」之间，而不是「服务端处理失败」。

最终定位到真实根因是**板子到电脑这条 Wi-Fi 链路的成功率只有 10~20%**
（`ping` 0% 丢包，但延迟在 3 ms ~ 770 ms 之间剧烈抖动；`SNTP 对时失败`；
`esp-tls: select() timeout`；板子因此反复重启）。这是网络环境限制，**不是代码问题**，
换手机热点验证即可复现「同一份固件成功率大幅上升」。

这条排查路径被完整写进了 [`项目复盘与踩坑记录.md`](项目复盘与踩坑记录.md)，
并沉淀成技能 `esp-idf-sandbox-workflow`。

---

## 二、提示词原文归档（按时间顺序）

### 2.1 第 1 周 · 立项：从零到能上传

**【转述】起点需求**

> 用 ESP32-S3-EYE 板子读板载加速度计，通过 Wi-Fi 把数据传到我电脑上的服务端，
> 再用一个网页实时显示出来。

**AI 的产出**：仓库骨架（`firmware/` + `server/` + `tools/`）、QMA7981 驱动、
纯标准库的 Python 服务端、单页展示页面。对应提交：

```
fc48008  初始版本：ESP32-S3-EYE 加速度计采集 + Wi-Fi 上传（不含摄像头）
```

**这一轮里人补的关键信息**（AI 无法自行获得）：

- 板子是 **N8R8（Octal PSRAM）**，不是普通 N8；
- Wi-Fi **只能连 "431"**（2.4G 主网，10.1.41.x，与电脑同网段），
  连 "432"（5G/IoT，10.1.42.x）会被隔离、回包不通；
- 电脑是**有线** 10.1.41.151，没有 Wi-Fi 网卡，所以服务器必须监听 `0.0.0.0`。

> **规律一：AI 不知道你的物理环境，凡涉及"哪块板子、连哪个网、哪台机器"的约束，
> 必须在提示词里一次性说清。** 这三条信息说清之后，后面所有网络相关的代码都没有返工。

### 2.2 第 1 周 · 摄像头接入（本项目最典型的一次踩坑）

**【转述】提示词**

> 板子上还有一颗 OV2640 摄像头，把拍照功能也加上，拍到的图直接无线传到电脑上显示。

**AI 第一版的方案（错的）**：给摄像头单独建一条 I2C/SCCB 总线，写死引脚号。

**实际现象**：一开摄像头，**加速度计也跟着读不出来了，两类数据全部不上传**。

**根因（AI 事后自查才发现）**：ESP32-S3-EYE 上 OV2640 的 SCCB **与 QMA7981 共用 GPIO4/5**。
按常规写法新建 I2C 端口 1 会**抢占 GPIO4/5**，把加速度计那条总线顶掉。
正确做法是让摄像头**复用**加速度计已建好的 `I2C_NUM_0`：

```c
/* camera.c —— 关键三行，缺一不可 */
.pin_sscb_sda = -1,          /* -1 = 不新建总线，复用已有的 */
.pin_sscb_scl = -1,
.sccb_i2c_port = I2C_NUM_0,  /* 明确指定复用哪一条 */
```

对应提交与文档：

```
5b2738d  feat: 新增 OV2640 摄像头采集 + 无线实时传图
8b836a8  fix: 修正不存在的 SCCB 配置项，改为真正生效的 CONFIG_SCCB_* 端口 0
```

完整方案见 [`摄像头功能方案与Git提交.md`](摄像头功能方案与Git提交.md)。

> **规律二：让 AI 自己解释"为什么这样接线"，比让它直接给代码更有效。**
> 这次事故之后，提示词里多了一条要求：**凡涉及外设引脚/总线，先说明板子上谁和谁共享什么，
> 再动手写代码。** 后面接电量计、加陀螺仪时，这条要求直接挡掉了同类问题
> （见 §4 案例 3：六轴陀螺仪的判断）。

### 2.3 第 1 周 · 构建环境：`idf.py` 不编译

**【转述】提示词**

> idf.py 在 Git Bash 里跑不起来，只打印一行 MSys 警告就退出了，什么都没编译。

**AI 的排查结论**（这一条是 AI 主动查出来的，人只提供了现象）：

沙箱环境缺 `MSYSTEM` / `PROCESSOR_ARCHITECTURE`，`IDF_TOOLS_PATH` 指错，
且 `ESP_ROM_ELF_DIR` 没设。于是写了一个启动器 `.idf_launch.py`：

- 剔除 `MSYSTEM`；
- 设 `IDF_TOOLS_PATH=C:/Espressif`；
- 补 `PROCESSOR_ARCHITECTURE=AMD64`；
- 自动取 `C:/Espressif/tools/esp-rom-elfs` 下最新版本目录（如 `20241011`）填进 `ESP_ROM_ELF_DIR`；
- 把工具链 `bin` 注入 `PATH`，再调 `idf.py`。

**后来又发现第二个坑**：CMake 全量 configure 会删上千个文件，触发环境的
「单轮删除超过 50 个即拦截」保护，报错却是极具误导性的
`ranlib: unable to copy file 'libespcoredump.a'; reason: Permission denied`。
真身报错是：

```
[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":1372,"threshold":50,"scope":"turn"}
```

**解决办法**：编译/烧录放到沙箱外执行；若 `build` 仍秒退，先单独跑一次
`idf.py reconfigure` 再 `build`。

> **规律三：报错信息和真实原因经常不是一回事。**
> 提示词里明确要求 AI「**不要停在第一个报错上，往下找有没有更早、更根本的报错**」，
> 这条要求直接命中了上面这个案例。

### 2.4 第 2 周 · 远程下发拍照指令（本题核心）

**【原文】**（这是本作业最关键的一条提示词，逐字保留）

> 远程下发拍照指令，要求开发板拍照后上传照片到服务器并在web平台显示。
> 还有我上面说的内容都做了吗？

随提示词一起提供了**两张参考实现的截图**（照片画廊、Retention 保留策略、
定时自动抓拍、硬件门禁、六轴 Gyro + 3D 姿态、电量计、报文监视等），
以及**一段 15 秒的参考实现演示视频**（`EgoLink DevBench` 控制台的传感器示波器页）。

> 那段视频 9/18 当天**没能看** —— 本机没有 ffmpeg，Python 侧也没有 cv2 / imageio-ffmpeg / av，
> 当时如实告知了用户。9/21 补装 ffmpeg 后抽帧逐屏读完，
> 观察与更正另见 [`参考产品对标分析-EgoLink-DevBench.md`](./参考产品对标分析-EgoLink-DevBench.md)。
> **教训：环境里缺工具时，"看不到"要当场说清楚，不能拿截图里的部分信息假装看全了。**

**这条提示词的信息密度很高**，拆开看它其实同时提了三件事：

1. **功能定义**：远程下发 → 板子拍照 → 上传服务器 → 网页显示；
2. **对账要求**：「我上面说的内容都做了吗」——要求 AI **逐条对照此前的承诺做自查**，
   而不是重新讲一遍设计；
3. **隐含的对标**：截图代表一个「更完整的参考实现」，需要判断哪些能抄、哪些抄不了。

**AI 的回应方式**（这一步值得记录）：没有直接说「做了」，而是先做了一次**逐条对照**，
分三类列出：

| 类别 | 内容 |
|---|---|
| ✅ 已做 | 指令通道、状态机、三条证据、防旧值冒充的两道闸、与周期上报解耦 |
| ❌ 没做 | 计划书 4.5 承诺的**哈希留痕 + 配额清理**、**断网 Flash 缓存**；截图里的**照片画廊**、**定时自动抓拍**、**硬件门禁** |
| ⚠️ 做不了 | 截图里的**六轴 Gyro 与 3D 姿态 Yaw**、**MAX17048 电量计** —— 因为 **ESP32-S3-EYE 板载的 QMA7981 只有加速度计、没有陀螺仪**，板子上也没有电量计芯片 |

> **9/21 更正（看完演示视频后）**：上面这行 ⚠️ 的范围**说宽了**。
> 视频里姿态标签旁边跟着一个 `1.02G` —— 那是合加速度的模长，
> 说明**四档姿态分类（侧边直立/竖直正面/自由倾斜/平放背面朝上）是纯靠重力方向判出来的，
> 加速度计就够了**；`Roll` / `Pitch` 同理。
> 真正做不了的只有：**Gyro 三路波形**、**绕重力轴自转的 Yaw**。
> 详见 [`参考产品对标分析-EgoLink-DevBench.md`](./参考产品对标分析-EgoLink-DevBench.md) 第四章。
> **教训：「做不到」必须给到信号/通道级，不能停在模块级** ——
> 停在"3D 姿态做不了"，会把本来做得出来的姿态分类一起误杀。

> **规律四：对「都做了吗」这类问题，唯一正确的回答方式是把清单摊开逐条对账，
> 而不是笼统地说"基本完成了"。** 这份对账表直接变成了下一轮的施工单 ——
> 用户下一句就是「做」。

### 2.5 第 2 周 · 收尾与考核准备

**【原文】**（逐字保留）

> 做，外壳、结构、PCB都可以使用AI实现。考核需要检查你的github仓库，仓库中应保留
> 从学期初开始到学期结束的完整提交记录。包括与AI的对话、主要提示词、项目代码、
> 技术及设计相关的各种文档。

**这条提示词里三个可执行的指令**：

1. **「做」** —— 承接上一轮对账表，把 ❌ 那几项补齐；
2. **「外壳、结构、PCB 都可以使用 AI 实现」** —— 明确授权把硬件外壳/结构/PCB 的
   设计工作也交给 AI，产出设计文档；
3. **「仓库要保留完整提交记录」** —— 约束提交方式：**必须多次迭代提交、每条附清晰说明**，
   不允许一次性压成一个「大提交」，也不允许改写历史。

对应本轮产出（3 笔提交，按「先修 bug，再加功能，最后改文档」的顺序拆开）：

```
20760cd  fix(server): 回补 _send_json 漏写的响应体，并让自测脚本绕开本机代理
289d679  feat(server): 照片画廊与数据配额——元数据与哈希永久保留，原图按天清理
d05e32f  feat(web): 新增照片画廊区块；docs 补画廊/配额说明与两条排错经验
```

**【原文】**（过程中另有一条关于输出长度的纠正，逐字保留）

> Your previous response was truncated due to output length limits. Break the operation
> into multiple smaller tool calls. Do NOT repeat the same oversized tool call.
> Break it into smaller operations.

**这条提示词的效果**：AI 之前试图用一次超大编辑改完整个服务端，被截断后反复重试同一个大调用。
收到纠正后改为**小步编辑**：一次只改一个函数、一次只加一个接口，每步单独验证。
结果是本轮新增的哈希/画廊/配额三项功能，全程没有出现过一次「改到一半文件损坏」。

> **规律五：对 AI 说「把大操作拆成小步骤」，是一条性价比极高的提示词。**
> 大编辑一旦被截断，重试的成本远高于一开始就分步。

### 2.6 穿插进行的短指令

这几条都极短，但每一条都改变了工作方向。记在这里是为了说明：
**AI 需要自己去补上下文，而不是反问「您指的是什么」。**

| 提示词原文 | AI 需要自行补上的上下文 | 实际动作 |
|---|---|---|
| `我插上板子了com4` | 板子已连 COM4、可以烧录了；此前一直在等硬件就位 | 执行编译 → 烧录 → 串口监视 → 真机联调 |
| `请继续执行任务` | 上一轮被中断，需要先读记忆与提交历史找回状态 | 读工作记忆 + `git log`，恢复上下文后继续 |
| `开代理了` | `github.com` 直连被封，Clash 已在 `127.0.0.1:7897` 就绪 | 走代理执行 git 远端操作 |

> **规律六：指令越短，AI 越应该先去查（读记忆、翻 git、看日志），而不是先问。**
> 本项目的用户偏好就是「结论先行、少解释、别反复确认」，所以「回来的是答案，不是问题」
> 本身就是一条硬要求。

### 2.7 第 3、4 周 · 一次给全四周课表，让 AI 自己排期

第 3、4 周的启动方式很典型：**没有逐条下指令，而是把完整的四周课程表贴进来**。

**提示词原文（节选，课程表部分逐字保留）**：

```
周次	专题/功能增量	主要教学内容与课堂实践	理/实	成果与证据

第3周	按键触发与物理反馈闭环	…增加"按键发起教学求助测试消息"；讲解本地确认、VPS接收和查看者回应的区别，
绘制简要流程并明确取消。…复用板端上传与 VPS 任务能力，增加按键/触摸、屏幕/灯光/蜂鸣，
实现触发—本地反馈—远端显示—回应/取消；同伴操作走查并完成首次三课接口检查。
成果与证据：既有工程新增按键与实体反馈；本地、VPS和查看者状态可区分；走查记录。

第4周	用自然语言查询与请求采集	…区分开发助手与产品运行时模型；接入授权语言服务，
讲解意图、参数和结构化输出，把自然语言分别映射为读取已有记录或请求一次新采集。
…将学生自建查询/采集接口封装为受限工具，校验参数和设备范围，处理歧义；
基于真实结果回复来源、时间与状态，无设备完成证据时不回复"已采集成功"。
成果与证据：自然语言分别调用已有查询/采集接口；结构化校验、歧义与无响应记录。

已经做完了第一二周的任务了，做完三四周的
```

**这条提示词为什么有效**：

1. **给的是验收标准，不是实现步骤。** 课表里写的是"成果与证据"，不是"你要写哪个函数"。
   实现路径留给 AI 自己找 —— 这正是它擅长的地方。
2. **保留了原文，没有转述。** 课表里那些短语（"本地确认、VPS接收和查看者回应的区别"、
   "无设备完成证据时不回复'已采集成功'"）**本身就是设计要求**，转述一遍就会丢信息。
3. **末句划定了范围**："已经做完了第一二周"—— 明确告诉 AI 不要重做，只做增量。
   项目从一开始就定下"后三周不新建工程，在同一套固件和同一个服务端上叠加"，
   这句话把这条约定又钉了一次。

**AI 从这条提示词里自行推出的几件事**（都没被明说）：

| 课表里的一句话 | AI 自行推出的设计决策 |
|---|---|
| "本地确认、VPS接收和查看者回应的区别" | 状态必须**拆成三列**（device/server/viewer），压成一个就是重演第 2 周的"旧值冒充" |
| "绘制简要流程并明确取消" | 取消要分两条路径（板子取消 / 查看者取消），且**已回应的不能取消** |
| "增加按键/触摸、屏幕/灯光/蜂鸣" | 先查乐鑫官方 BSP 确认引脚；发现板上**没有蜂鸣器**，LCD 字节序需真机确认 → **诚实说明不做 LCD 的理由** |
| "区分开发助手与产品运行时模型" | 系统提示词必须写全规则 —— 运行时模型看不见仓库、看不见开发期对话 |
| "无设备完成证据时不回复已采集成功" | 不能只靠提示词 → 做成**工具层 + 出口守卫**的机制 |
| "处理歧义" | 设备不唯一 → 返回 `needs_clarification` + 候选清单，**要求反问，不许猜** |

> **规律七：把"验收标准"原文给 AI，而不是把"实现步骤"给它。**
> 前者让它自己做设计权衡，后者把它降级成打字员。

### 2.8 第 3、4 周 · 穿插的短指令

| 提示词原文 | AI 需要自行补上的上下文 | 实际动作 |
|---|---|---|
| `Please continue with the conversation based on the summarized context above.` | 上一轮上下文被压缩过，需要先读工作记忆与文件现状再动手 | 读记忆 + 列目录 + 跑自测，先确认状态再继续 |

> 这一条再次印证规律六：**上下文丢失时，第一件事是"查"，不是"问"。**

### 2.9 第 5 周 · 看完教师参考实现视频后拍板

第 5 周的起点不是课程要求，而是老师演示的一段参考实现（EgoLink / DevBench）视频。
我把视频逐屏看了一遍，让 AI 做了一件事：**先对账、再动手**。

> **提示词（原文）**
>
> 我把参考实现的演示视频逐屏看完了，截图和观察记在
> `参考产品对标分析-EgoLink-DevBench.md`。请你：
> 1. 先做**能力对账** —— 它有哪些功能、我们哪些能做哪些不能做，逐条给依据；
> 2. **特别检查 9/18 那份对账表有没有把范围划错** ——
>    当时写的"六轴 Gyro 与 3D 姿态都做不了"，你确认一下这句话准不准；
> 3. 如果要做，给一个**最小可行范围**，并指出需要先决定的架构问题。

AI 的产出里有两点是它自己发现的、我原本没想到的：

- **9/18 那句话划宽了**：做不了的只有 Gyro 三路波形和 3D 孪生的 Yaw，
  而**四档姿态分类恰恰是加速度计最擅长的活** —— 视频里姿态标签旁边的
  `1.02G` 就是证据（那是合加速度模长，说明它也是拿重力方向判的）。
  收窄之后，本来要被误杀的功能被救回来了。
- **架构上有个前置决定**：参考实现是 20Hz 实时推送，
  而我们的链路实测延迟在 3ms~770ms 剧烈抖动 ——
  所以要么"板端攒批上传"、要么"换成 WebSocket 长连接"。
  AI 明确给了倾向和理由（**批量上传对丢包天然容错**），而不是把选择题丢回给我。

我据此拍板：

> **`做，做完就上传github`**

（就是这一句。范围、架构、验收标准都已经在上一轮定完了，
所以这里只需要一个"做"字 —— 这也是规律三说的：**把验收标准给全，拍板就可以很短。**）

### 2.10 第 5 周 · 穿插的短指令

| 提示词原文 | AI 需要自行补上的上下文 | 实际动作 |
|---|---|---|
| `做，做完就上传github` | 范围在上一轮文档第五章，含两个前置决定 | 服务端 + 固件 + 网页 + 自测 + 文档 + 推送 |
| `请继续执行任务` | 无 | **AI 自己发现并补掉了一处遗漏**（见下） |

### 2.11 第 5 周 · 一句"继续"，AI 自己去找没做完的事

第 5 周推送完之后，我只说了四个字：

> **`请继续执行任务`**

这句提示词**没有指定任何内容** —— 没有说"去修什么"、也没有说"还差什么"。
AI 的处理方式是：**不看"还剩什么功能没写"，而是看"已经写完的东西之间有没有对不上"**，
结果找到了第 5 周交付里的一处遗漏：

> 第 5 周新加的波形/姿态数据，**第 4 周的自然语言问答看不见** ——
> 同一个页面，网页画得出来，用一句话问不出来。

它随后自己把三处补齐（设备白名单扫 `wave_batches`、加 `get_attitude` / `query_waveform`
两个只读工具、系统提示词补第 6 条铁律），并加了一组新自测（第 18~21 组）
和一个新的出口守卫 `yaw_fabricated`。

**这里值得记的是"怎么找"这件事。** 无约束的"继续"很容易变成
"再写点新功能" —— 那样只会让系统更宽，不会让它更一致。
AI 用的是**交叉核对**：把新加的数据，对照所有已有入口逐项比一遍
（白名单？工具有吗？提示词写了吗？）。这条方法本身后来被写进了复盘文档 §13.9，
成了"加一张表时要同时问三件事"的检查清单。

> **一句话：`继续` 这种无约束指令，应该被理解成"去把不一致的地方找出来"，
> 而不是"再写点新东西"。**
> 判断标准：**这次改动是让系统更宽，还是让它更一致？**
> 前者是加法，后者才是收尾。
> （这条的完整教训见 §四 案例 13。）

### 2.12 第 5 周收尾 · 还是"继续"，这次去翻计划书的欠账

同一句提示词又用了一次：

> **`请继续执行任务`**

上一轮（2.11）AI 找的是"新数据接进老入口"，这一轮它换了个方向：
**去对照计划书原文，逐条核"承诺过的是不是都落地了"**。

它翻到第 2 周计划书 4.2 节，那里有一句被加粗的话：

> **断网缓冲不是可选项** —— 讲义参考实现里也提到"断网期间捕获帧自动驻留 Flash 待恢复上传"。

而这条在落地清单里一直是没做的。AI 顺着往下看，发现了根因：

```c
if (!s_wifi_connected) {
    ESP_LOGW(TAG, "Wi-Fi 仍未就绪，跳过本次上传");
    continue;        /* ← 注释说"跳过上传"，实际跳过了整个循环体，连帧都不抓 */
}
```

**"断网那段时间的数据永久丢失"** —— 对一个无人值守的采集终端来说，
这和"设备只能在网络好的时候才有用"是同一句话。

于是补做了整套：分区表加 `backlog`（SPIFFS 3 MB）、新增 `backlog.c/h`（文件名即索引的
环形队列）、主循环接线（断网按 10 秒抓帧入队、联网按 FIFO 补传）、
服务端接受补传帧并**硬拒**"补传 + `request_id`"的组合、画廊把三个时间分开显示、
新自测 9 组 72 条断言、新文档《断网补传设计说明.md》。

**这一轮值得记的是"翻承诺"这个动作。** 计划书是**几个月前的自己**写的，
里面标成"不是可选项"的东西，最容易在赶功能的时候被跳过 ——
因为它不在当前的待办列表里，而在一个已经翻过去的文档里。

> **一句话：`继续` 的第二种用法是"去翻自己以前答应过什么"。**
> 这一轮和上一轮的共同点是：**都没写新功能，都在补旧账。**
> 区别只是"旧账"藏在哪里 —— 上一轮藏在**代码之间**，这一轮藏在**文档里**。

> 顺带记一个数字：这一轮 AI 全程**没有问过一句"要不要做"**，
> 直接按计划书原话（"不是可选项"）判定为必做项就开工了。
> 这是对的 —— 计划书里已经写明优先级的事，再问一遍是浪费一轮。

---

## 三、有效提示词的写法规律

把上面六条规律收敛成可以直接复用的写法。**这套模板在本项目里是实测有效的**，
第 2 周之后的提示词基本都长这个样子。

### 3.1 六条规律速查

| # | 规律 | 反例（无效） | 正例（有效） |
|---|---|---|---|
| 1 | **物理约束一次说清** | 「让板子连网传数据」 | 「板子是 ESP32-S3-EYE N8R8；Wi-Fi 只能连 431（2.4G，10.1.41.x）；电脑是有线 10.1.41.151 无 Wi-Fi 网卡」 |
| 2 | **外设先讲共享关系再写代码** | 「加上摄像头功能」 | 「板上有 OV2640 和 QMA7981，先确认两者在总线/引脚上有没有冲突，再动手」 |
| 3 | **别停在第一个报错上** | 「这个报错怎么修」 | 「报错是 X，但请往下找有没有更早、更根本的报错，不要只修表面」 |
| 4 | **要清单，不要结论** | 「都做完了吗」 | 「逐条对照你之前承诺过的内容，分『已做/没做/做不了』三类列出来」 |
| 5 | **大操作拆小步** | 「把整个服务端改一遍」 | 「拆成小步骤，一次只改一个函数，每步单独验证」 |
| 6 | **短指令先自查再开口** | （AI 反问「您指的是什么」） | 「先读工作记忆和 git 历史找回上下文，再动手」 |

### 3.2 三条最有价值的提示词模板

**① 对账式（用于「做得怎么样了」）**

> 逐条对照你此前承诺/规划过的内容，做成表格：✅ 已做 / ❌ 没做 / ⚠️ 做不了。
> 每一条都要给出可复核的证据（文件路径或提交哈希），「做不了」要写清卡在哪。

*为什么有效*：把「进度汇报」从主观叙述变成可验证清单，并且天然暴露欠账。

**② 排障式（用于「跑不通」）**

> 现象是 ……（贴原始日志/截图）。
> 要求：① 先给出所有可疑点的排序，说明每个点的依据；
> ② 不要停在第一个报错，往下找更根本的那条；
> ③ 每个结论都要有证据，拿不到证据就写「待验证」。

*为什么有效*：明确要求证据与排序，能有效压制 AI 的「猜一个原因然后编一段解释」。

**③ 施工式（用于「开始做」）**

> 做。要求：① 拆成小步骤，一次只改一处，每步单独验证；
> ② 改动保持与现有代码风格/命名/注释习惯一致；
> ③ 完成后按逻辑拆成多笔提交，每笔附清晰说明。

*为什么有效*：第 2 条避免了「新代码风格和旧代码打架」，第 3 条直接满足考核对提交记录的要求。

---

## 四、AI 出错与人工纠正的 14 个案例

**这一节是本文档最重要的部分。** 用 AI 的关键不是它不犯错，而是**人能不能发现它错了**。

> 案例 1~13 都是"人发现 AI 错了"；**案例 14 是个例外 —— 那四个问题是 AI 自己在收尾时找出来的**，
> 所以它单独记了"AI 是怎么发现自己的问题的"。这两类都要有：
> 前者靠人，后者靠机制。

### 案例 1：摄像头抢走加速度计的总线（AI 方案错，人工从现象反推）

- **AI 的第一版**：给摄像头单独新建 I2C 端口 1，写死 SCCB 引脚号。
- **现象**：一开摄像头，**加速度计也一起读失败**，两类数据全断。
- **人工纠正**：没有直接改代码，而是先问「为什么开了摄像头会连带影响加速度计」——
  这个提问逼出了真正的原因：两者共享 GPIO4/5。
- **结论**：摄像头必须复用 `I2C_NUM_0`（`pin_sscb_sda/scl = -1`）。
- **教训**：**「两个功能互相影响」这类现象，一定要先怀疑资源冲突，而不是先怀疑代码逻辑。**

### 案例 2：AI 自己引入的 bug —— `_send_json` 漏写响应体（靠端到端自测抓到）

- **AI 干了什么**：为修「高频短连接下的连接回收」问题，把 `end_headers()` 统一换成
  `_end_headers_close()`，**顺手把紧随其后的 `self.wfile.write(body)` 一起弄丢了**。
- **现象**：所有 JSON 接口只回响应头、不回响应体。客户端报
  `IncompleteRead(0 bytes read, N more expected)`；
  而**服务端日志显示 200、毫无异常**。
- **为什么危险**：这个 bug **看日志永远发现不了**。如果只有函数级单元测试，
  也发现不了（`_send_json` 本身不报错）。
- **怎么抓到的**：AI 在跑端到端自测时发现「画廊接口返回 0 字节」，顺着
  `Content-Length` 与实际写出字节数不一致，定位到那一行。
- **修复**：提交 `20760cd`。
- **教训**：**自测必须跑真实 HTTP 往返，不能 mock 掉网络层。**
  `Content-Length` 声明了多少、实际写出多少，这两个数字要一起看。

### 案例 3：六轴 Gyro 与 3D 姿态 —— AI 没有顺着截图硬做（这次是对的）

- **背景**：参考实现截图里有「六轴 Gyro」和「3D 姿态 Yaw」，看起来是必须抄的功能。
- **AI 的处理**：没有直接开写，而是先回去核对板载传感器型号 ——
  **ESP32-S3-EYE 板载的是 QMA7981，只有三轴加速度计，没有陀螺仪**；
  板子上也没有 MAX17048 电量计芯片。
- **结论**：这两项**不是"没时间做"，是硬件上做不了**，如实标注 ⚠️ 而不是假装完成。
- **教训**：**AI 必须敢于说「这个做不到」并给出物理依据。**
  顺着截图硬编一个假的 Yaw 出来，是本项目里最严重的作弊 —— 恰好也是第 2 周题眼要防的东西
  （拿不出来的东西装作拿得出来，本质就是「旧值冒充」的另一种形态）。
- **9/21 后续更正**：这句话本身对，但**范围划宽了**。
  补看演示视频后确认：姿态分类与 `Roll`/`Pitch` 只需加速度计（视频里的 `1.02G` 就是证据），
  只有 **Gyro 波形**和**绕重力轴的 Yaw** 才真的做不了。
  → **"敢于说做不到"是第一步；第二步是把这个边界切准到信号级。**
  划宽了同样是错误 —— 它会让你把做得出来的功能也一起放弃。

### 案例 4：把网络问题误判成代码问题（方向性误判）

- **现象**：板子串口一路刷「连续上传失败 N 次」，服务端日志却持续有 201。
- **AI 最初的倾向**：怀疑服务端接口卡住 / 固件 HTTP 客户端写法有问题。
- **人工纠正的方式**：要求 AI **先把两边的原始数据摆在一起比对**
  （服务端 `readings` 表的条数与时间间隔 vs 板子日志的失败次数与时间点），
  而不是各自独立分析。
- **比对结果**：服务端各接口响应 30~50 ms 正常、帧上传 45 KB 用时 50 ms、日志不卡死 ——
  **服务端没有问题**。真正的问题是板子→电脑的 Wi-Fi 链路成功率只有 10~20%：
  `ping` 0% 丢包但延迟 3 ms~770 ms 剧烈抖动、`SNTP 对时失败`、
  `esp-tls: select() timeout`、板子反复重启（`boot_id` 涨到 boot9）。
- **结论**：**属网络环境限制，非代码可解。** 与第 1 周记录的「98% 请求失败率」同一根因。
  根治办法是换网络环境（如手机热点）验证。
- **教训**：**当两个系统的说法互相矛盾时，先做时间戳对齐与条数比对，再谈归因。**

### 案例 5：AI 误改函数签名，把能跑的代码改坏了

- **AI 干了什么**：新增 `http_drain_body()` 时，误把
  `upload_reading(const qma7981_sample_t *s, const char *ts_device)`
  改成了 `upload_data(const imu_sample_t *s, ...)`，并且函数定义被拆开。
- **怎么发现的**：编译直接报错（类型 `imu_sample_t` 不存在）。
- **修复**：分两步 —— 先还原签名与函数体开头，再把新函数挪到正确的注释段之后。
- **教训**：**"插入新函数"要用最保守的方式**：找准锚点、只插不改，
  不要顺手重排周围代码。

### 案例 6：配额清理的"删不掉死循环"

- **AI 第一版**：`os.remove()` 抛异常就 `continue`（下轮再试）。
- **问题**：某些环境下**文件已经删掉了才抛错**（例如回收站不可用）。
  这种情况文件已经没了，但 `purged_at` 永远不会被标记 →
  每轮扫描都会重新"清理"它一次，**陷入永远清不掉的死循环**。
- **修复**：`os.remove` 抛异常后**再查一次文件是否存在**，只有文件确实还在才算真失败：

```python
try:
    os.remove(path)
except OSError as e:
    if os.path.exists(path):     # 文件还在 = 真失败，下轮重试
        print("清理原图失败，下轮重试: %s" % row["filename"])
        continue
# 文件已经不在了（或删除成功）→ 正常标记 purged_at
```

- **怎么发现的**：自测脚本断言「清理了 2 张原图」失败，报 `0`，但
  「snapshots 里只剩 latest.jpg」却通过了 —— **两个断言互相矛盾，指向"删了但没记账"**。
- **教训**：**自测里互相矛盾的断言，往往比失败的断言更有价值。**

### 案例 7：AI 误删未跟踪文件（真实事故，如实记录）

- **AI 干了什么**：在用 `git filter-branch` 重写历史时，**误删了当时未被 git 跟踪的文件**。
- **处理方式**：如实报告，说明影响范围与恢复方案，不遮掩。
- **此后的硬规矩**：
  - 破坏性操作（删文件、重写历史、清目录）**先备份、先列清单、先要确认**；
  - 不使用 `git filter-branch` 这类高风险历史改写命令；
  - 未跟踪文件一律先 `git status` 确认，再动任何 git 清理命令。

> **本节小结**：7 个案例里，**AI 出错 4 次（案例 1/2/5/6）、方向误判 1 次（案例 4）、
> 判断正确 1 次（案例 3）、人为事故 1 次（案例 7）**。
> 真正让项目没跑偏的，不是 AI 一次就对，而是**每一步都有人复核、每一个结论都要证据**。

### 案例 8：AI 照抄索引，把服务端写得起不来（第 3 周）

- **AI 干了什么**：给新表 `help_events` 建索引和写查询时，照抄了 `commands` 表的
  `ORDER BY id DESC`，但新表主键是 `event_id`(TEXT)，**没有 `id` 列**。
  结果 `init_db()` 直接抛 `no such column: id`，**服务端启动即崩**。
- **怎么发现的**：自测脚本报"服务端未能启动，中止"—— 但它当时把服务端输出丢进了
  `DEVNULL`，看不到真正原因。手工起一次才看到 traceback。
- **AI 的修正（两处，不是一处）**：
  1. 改掉索引与查询里的排序字段（按 `received_at` 排，跟着业务上的"新旧"走）；
  2. **顺手改掉自测脚本的盲区** —— 服务端日志落盘，并在自测中断时把日志尾部打出来。
     否则下次还是"只看到断连接，原因烂在日志里"。
- **人工纠正/追加**：同类错误在查询接口里还有一份，运行时才炸。AI 补了
  **分发层统一异常兜底**（打印 traceback + 回 500 JSON），把这类"静默断连接"一次性堵住。

> **价值点**：AI 出错不稀奇，值钱的是它**把"为什么之前没发现"也一起修了**。
> 只修表象，下次换个字段还会再犯。

### 案例 9：★ AI 写的守卫误伤了自己守规矩的输出（第 4 周）

- **AI 干了什么**：为第 4 周写的"防假成功"出口守卫，第一版用**子串匹配** ——
  文案里出现"拍好了"这类词、且没有完成证据就拦下。
- **实测翻车**：用户问"让板子拍一张，然后**告诉我拍好了没**"，
  模型老老实实反问（"…再用 get_command_status 查结果**告诉你拍好了没**"），
  守卫把这句话判成"假成功"并改写了。
- **为什么这次值得单独记**：这是**AI 自己引入的 bug**，而且方向很微妙 ——
  守卫本来是为了"防止说假话"，结果**把真话也拦了**。
  一个会误伤的守卫比没有守卫更糟，因为它会让人不再信任它。
- **修正**：改成**按句子看语气**，不看有没有那几个字：
  句子里有 `？`、命中词后紧跟"没/吗/否"、或整句带否定/条件标记（不、没有、尚未、
  是否、告诉、确认、如果…），都不算声称。
- **回归防护**：往自测里加了 4 条断言专门守这个误伤场景
  （复述用户疑问、主动否定成功、真正声称仍被拦下、原文未被改写）。

> **规律八：任何基于关键词的守卫，都要问一句"这句话被复述一遍会怎样"。**
> 用户的问题里本来就带着那些关键词，而模型复述用户的话是最自然的行为。

### 案例 10：★ AI 只按"自己造的世界"验收，把最优路径写成了死路（第 5 周）

- **背景**：波形的时间轴要回答"样本之间隔多久"。AI 设计了三条路里最合理的两条：
  优先读板端采样时刻，读不到再退回服务端接收时刻。
- **AI 的说法**：自测 11 组全绿，时间轴单调、末端贴"现在"、间隔精确等于 1/hz。
- **人的追问**：**"这块板子的 SNTP 实测经常对不上时"这件事，你考虑了吗？**
- **问题**：AI 的自测数据是它自己造的 —— 板端时刻**总是可解析**的。
  而真机上 SNTP 失败时，时间戳写成 `uptime+12.345s(time_not_synced)`，
  服务端**解析不出来**。也就是说：**最优路径在这台设备上永远走不到，
  实际会掉到最差的那条（服务端接收时刻，含网络抖动）。**
- **修正**：补上第三条路 —— **批号连续 + 采样率已知 → 完全不需要任何时钟，直接反推**。
  并加了一条断言：反推出来的时间轴与"板子对时了"那条路**逐点相同**。
- **回归防护**：自测第 7b 组专门覆盖三档（`device` / `derived` / `server`）
  以及两种降级条件（批号断档、板端如实报了丢样本）。

> **规律九：验收标准不能是"我的自测过了"，必须是"这条路径在真设备上会不会被走到"。**
> 如果不会 —— 那这条最优路径是写给谁看的？
> 这类错误有个很隐蔽的特征：**所有测试都是绿的**，因为测试数据也是同一个 AI 造的。

### 案例 11：★ 几何映射"数值全对、画面全错"（第 5 周）

- **背景**：3D 姿态孪生要把板子的实际朝向画出来。
- **AI 的第一版**：`pitchCss = -asin(uy)` / `rollCss = asin(ux)`，
  然后 `rotateY(rollCss) rotateX(pitchCss)`。
- **AI 的说法**：数值验证通过 —— 平放算出来 0°、竖立算出来 ±90°。
- **问题**：**画面是错的。** 那两个角是**相对世界坐标系**算出来的，
  却被塞进了一个已经被 `rotateX(58deg)` 转过的父层里，语义错位。
  竖直正面时孪生体绕 Y 转了 87°（立起来了但朝向乱），侧边直立几乎看不出来。
- **怎么发现的**：不是靠断言，是靠**截图看渲染结果**。
- **修正**：不再套公式，改成**解方程** `R · g设备 = (0, 0, −1)`（世界坐标系的"下"），
  两个自由度刚好够：
  ```js
  var phi   = Math.atan2(gx, gy) * 180/Math.PI;        // 绕桌面竖轴
  var theta = Math.atan2(r1, gz) * 180/Math.PI + 180;  // 绕 X 轴压下去
  ```
  手工代入验证：`g = (0.02, -0.71, 0.70)` → `R·ĝ = (0.0002, 0.0002, -0.9973)` ✓
- **回归防护**：`selftest_page.js` 第 6 组改成断言**渲染出来的 `transform`**，
  而不是中间变量。

> **规律十：几何 / 布局类改动，断言数值不算验收 —— 必须看渲染出来的结果。**
> 数值对而画面错，是因为"算出来的角属于哪个坐标系"这件事，
> 数值本身看不出来。

### 案例 12：★ AI 把"只能假设"的东西写成了断言（第 5 周）

- **背景**：姿态分类里要输出"正面朝上"还是"背面朝上"。
- **AI 的第一版**：直接写 `"背面朝上" if uz > 0 else "正面朝上"`，并配了一句
  "注意方向：重力沿设备 +Z 意味着 +Z 那一面朝下"。
- **人的追问**：**"这句话是推导出来的，还是你猜的？"**
- **问题**：这句话把**两件事**混成了一件：
  1. **能 100% 确定的**：静止时加速度计测的是**比力**（`f = a − g`，`a=0`），
     读数指向天空 —— 所以 `az > 0` ⟺ 传感器 +Z 轴朝上。**与芯片怎么贴装无关。**
  2. **只能假设的**：+Z 对应板子的哪一面。**乐鑫没公布，我们也没板子实测。**
  AI 把第 2 条当成了第 1 条的推论写出来，读起来像物理定律，其实是猜的。
- **修正**：把假设提成显式常量 `Z_UP_IS_FRONT_FACE = False`，
  数值照算，只有文案跟着它变；注释里写明"拿到板子平放一次即可确认，改这一行就行"。
- **回归防护**：加了一条自测验证"这句承诺没吹牛" ——
  翻转常量后文案整体翻转，而姿态分类 key 不变。
- **附带发现**：这个错**特别难靠自测抓** ——
  写反了的话所有数字都对、只有文案错。原来那条自测只断言 `posture` 这个 key
  （`flat`），怎么写反都能过。现在连"说明文字里写没写明哪一面"也一起断言了。

> **规律十一：AI 会把"猜测"和"推导"写成同一个语气。**
> 所以看到任何带"因为…所以…"的注释，都要问一句：
> **这一步是算出来的，还是它默认的？**
> 分不清的地方，就把它做成一个能一行改掉的显式常量 ——
> 而不是埋在逻辑里，让下一个人猜为什么是反的。

### 案例 13：★ AI 加完新数据，忘了"同一页面的老功能看不见它"（第 5 周）

- **背景**：第 5 周加了 `wave_batches`（三轴波形 + 姿态），服务端接口、网页示波器、
  3D 孪生、自测、文档全部做完，编译通过、六个自测全绿。
- **问题**：**同一个页面上的第 4 周自然语言问答看不见这块数据。**
  网页上画得出来的东西，用一句话问不出来。
- **性质**：这不是 bug，是**遗漏** —— 而遗漏比 bug 难发现得多，因为它**不报错**。
  编译通过、自测全绿、页面正常，一切指标都是绿的。
- **根因**：只想到了"新功能自己要能跑通"，没想到"已有功能应该能看见它"。
  具体漏三处：
  1. `known_devices()` 没扫 `wave_batches`
     → 只上传波形、没传 `readings` 的设备被判成**"根本没这台设备"**；
  2. 工具清单里没有相关工具 → 模型只能说"我查不到"；
  3. `SYSTEM_PROMPT` 里没写姿态口径 → **查得到也会说错**。
- **第三处是真正的教训**：前两条是"能不能查到"，第三条是"**查到了会不会说错**"。
  而它最容易漏，因为它不在代码里、在**语义**里：

  > 本板无陀螺仪/磁力计，**航向在原理上不可测**。

  这句话在文档里写过，但**运行时模型看不见文档** ——
  它只看得见工具返回的 JSON 和系统提示词。所以模型面对"板子朝哪边"，
  只剩留空或编数两条路，而编出来多半是 `0°`（`0°` 就是正北，等于凭空造了个方向）。
- **修正**（三处补齐 + 一道新守卫）：
  ① `known_devices()` 加 `("wave_batches", "device_id")`；
  ② 加 `get_attitude` / `query_waveform` 两个**只读**工具（受限工具 7 → 9 个，
     唯一会改状态的仍然只有 `request_capture`）；
  ③ `SYSTEM_PROMPT` 补第 6 条铁律（姿态由重力判定 / 能测与不能测分开说 /
     姿态是"某一批的属性"而非"设备永久状态" / 波形会被滚动清理）；
  ④ 出口守卫加第 4 道 `yaw_fabricated`：**把不可测的量说成测到了**就拦下。
- **回归防护**：自测从 17 组扩到 21 组。第 18 组专盯"只上传波形的设备算不算已知设备"，
  第 21 组盯航向守卫，且**同时断言"不误伤"**
  （如实说不可测不拦、没问航向时报俯仰/横滚不拦）。
- **实测结果（最值得记的一点）**：真模型问"板子朝哪个方向，航向是多少"，
  **自己就答对了** —— "航向不可测，本板没有陀螺仪和磁力计，绕重力轴自转不改变重力方向，
  这个自由度从数据里根本不存在"，**守卫未触发**。

> **规律十二：守卫被触发，说明的从来不是"守卫很强"，而是"上游没交代清楚"。**
> 这次守卫一次都没响，才是想要的结果 —— **口径写在提示词里是有用的。**
> 反过来看：如果一个守卫天天在响，该改的不是守卫，是提示词或者工具返回值。

> **规律十三：加一张表，不是加一个接口。**
> 要同时问三件事 ——
> **① 按 `device_id` 聚合的地方（白名单/权限/歧义判断）扫了吗？
> ② 对外入口有了吗？③ 这块数据有哪些"只能这么理解"的约束，写进提示词了吗？**
> 第 ③ 条列成清单，就是这个项目一路攒下来的那些"不可互推"：
> `UPLOADED ≠ COMPLETED`、三层状态不可互推、`dropped` 非 0 不能反推时间轴、
> 以及这次的"航向不可测 ≠ 没采到"。

> **中段小结（案例 8~13，第 3~5 周）**：6 个案例里，
> **AI 写错 2 次（案例 8 照抄索引、案例 9 守卫误伤）**、
> **只按自己的假世界验收 3 次（案例 10 时间轴、案例 11 几何映射、案例 12 假设当断言）**、
> **遗漏 1 次（案例 13 新数据没接进老功能）**。
>
> 值得注意的变化：**第 3 周之后，AI 的错误从"写出跑不起来的代码"
> 变成了"写出跑起来但语义不对的东西"。**
> 前者编译器和自测能挡，后者挡不住 ——
> 只能靠"看真机会走哪个分支""看渲染出来的画面""读自己写的断言在测什么"
> 这三种检查（详见 `项目复盘与踩坑记录.md` 各周小节末尾）。

### 案例 14：★ AI 定义了宏却没接线，还留了个"跳过整个循环体"的 `continue`（第 2 周欠账补做）

- **背景**：顺着 2.12 那条"翻计划书欠账"的线索，AI 补做计划书 4.2 的断网缓冲。
  计划书原话是「**断网缓冲不是可选项**」，但一直没做。
- **问题一：`continue` 的作用范围比注释说的宽。** 原来的写法是：

  ```c
  ESP_LOGW(TAG, "Wi-Fi 仍未就绪，跳过本次上传");
  continue;      /* 注释说"跳过上传"，实际跳过了整个循环体 */
  ```

  注释写的是"跳过本次上传"，读起来完全合理 —— 但 `continue` 跳过的是**整轮循环体**，
  后面抓帧的代码也一起被跳过了。于是**断网那段时间一张图都没有**，
  而抓帧（`camera_capture()`）根本不需要网络。

- **问题二：一个子系统的故障悄悄让另一个子系统也不工作。** AI 第一版把抓帧缓存
  放在 `qma7981_read()` **之后**（"顺手"），但那里有一条 `continue`：
  加速度计一旦读失败，**整轮会把抓帧也停掉**。串口上只看得到「读取传感器失败」，
  排查时根本想不到是它连累的。

- **问题三：`BACKLOG_ENABLE` 是个死开关。** 宏定义了、注释写了，但
  **代码里没人读过它** —— 比没有这个宏更糟，因为它让人以为关掉就生效了。
  接线时 AI 特意让它返回 `ESP_ERR_NOT_SUPPORTED` 而不是"成功"：
  返回成功的话，断网时日志会显示"已缓存一帧"而其实什么都没存。

- **问题四：`sscanf` 判据不够。** 校验队列文件名时用
  `sscanf(name, "%6lu_%8lu_%8lu.%3s", ...) != 4`，但 `%3s` 读完 `jpg` 就停了，
  于是 `…jpg_extra` 也会被判成合法。它混进队列后会**永远排在队首**，
  每次补传都失败 —— 日志上只是"补传一直失败"，完全看不出根因在文件名解析。
  修法是加 `%n` 拿消费长度，再跟 `strlen` 比。

- **这四个问题有一个共同点：都不会让编译失败，也不会让自测变红。**
  编译 0 警告 0 错误、当时已有的六个自测全绿，但行为是错的。
- **AI 自己怎么补的**：把"队列顺序"和"淘汰策略"这两条不变量
  **写成主机上可执行的自测**，并且让自测**去读固件源码做对账** ——
  第 7 组读 `backlog.c` 的 `snprintf` 格式串、读 `app_config.h` 的
  `BACKLOG_MAX_FRAMES`，谁改了 C 那边而忘了 Python 镜像，这一组会红。
  文档里也明确写了"这两组不是在跑固件代码"，**不假装它测了固件**。

> **规律十四：跳转语句（`continue`/`break`/`return`）的注释要写"跳过了什么范围"，
> 不是写"意图"。** "跳过本次上传"这句话会让人以为只影响上传 ——
> 而实际跳过的是后面所有代码。**改这类代码前，先看它前面有几条早退。**
>
> **规律十五：加一个配置宏之后，搜一遍它被谁读过。**
> 只有定义处出现，那它就是个死开关 —— 而"看着能关、其实关不掉"比没有开关更危险。
>
> **规律十六：一条不变量如果破了不报错，就该把它变成断言。**
> 队列顺序、淘汰计数这类东西，错了是静默的。主机上钉死一条，
> 比在板子上肉眼找便宜得多 —— 前提是**在文档里写清楚它测的是什么**。

> **本节小结（案例 14）**：这个案例里 AI **没有被人工纠正过一次** ——
> 四个问题全是它自己在收尾核对时找出来的。
> 但要注意：**它找出来的方式不是"更仔细地读代码"，而是"把约定写成断言"。**
> 人肉读代码只能发现"写得不对"，发现不了"定义了一个没人用的东西"。
>
> 全节合计（案例 1~14）：**AI 写错 2 次、只按自己的假世界验收 3 次、
> 遗漏 1 次、自己给自己埋坑 1 次**，其余 7 次是"AI 处理得当"的记录。
> 值得注意的趋势：**越往后，AI 的错误越"安静"** ——
> 从"编译不过"到"跑起来但语义不对"，再到这一轮的
> "编译通过、自测全绿、行为还是错的"。

---

## 五、产出物索引：每条提示词 → 提交 → 文件

| 阶段 | 关键提示词（摘要） | 提交 | 主要文件 |
|---|---|---|---|
| 第 1 周立项 | 读加速度计 + Wi-Fi 上传 + 网页显示 | `fc48008` | `firmware/main/main.c`、`qma7981.c`、`server/server.py`、`server/index.html` |
| 第 1 周摄像头 | 加上 OV2640 拍照上传 | `5b2738d`、`8b836a8` | `firmware/main/camera.c`、`camera.h`、`摄像头功能方案与Git提交.md` |
| 第 1 周排障 | idf.py 不编译；报错极具误导性 | `0af55de`、`6c77b0b`、`1f442d9` | `tools/run_idf.py`、`项目复盘与踩坑记录.md` |
| 第 2 周设计 | 远程下发拍照指令 + 逐条对账 | `fcfa37f`、`e970d75` | `server/server.py`、`第二周作业-项目计划与四课实施方案（版本迭代稿）.md` |
| 第 2 周前端 | 网页要能下发指令并显示本次结果 | `b8b3a7d` | `server/index.html` |
| 第 2 周固件 | 板子要能收到指令并回执 | `8762f95` | `firmware/main/main.c`、`app_config.h` |
| 第 2 周文档 | README 补接口一览与课堂验证步骤 | `bee01d4` | `README.md` |
| 第 2 周排障 | 连接回收问题 | `27ba443` | `server/server.py`、`firmware/main/main.c`、`sdkconfig*` |
| 第 2 周收尾 | 「做」+ 考核要求 | `20760cd`、`289d679`、`d05e32f` | `server/server.py`、`server/selftest_gallery.py`、`server/index.html`、`README.md` |
| 第 3 周 | 完整四周课表 + `做完三四周的` | 见下方提交历史 | `firmware/main/help_btn.c`、`help_btn.h`、`app_config.h`、`main.c`、`server/server.py`、`server/selftest_help.py`、`server/index.html`、`第三周-按键触发与物理反馈闭环.md` |
| 第 4 周 | 同上（一次给全四周，AI 自行排期） | 见下方提交历史 | `server/nl_agent.py`、`server/selftest_nl.py`、`server/selftest_page.js`、`server/server.py`、`server/index.html`、`第四周-用自然语言查询与请求采集.md` |
| 第 5 周收尾 | `请继续执行任务`（AI 自行交叉核对，补掉一处遗漏） | `9d5938e` | `server/nl_agent.py`（+2 只读工具、+第6条铁律、+`yaw_fabricated` 守卫）、`server/selftest_nl.py`（17 → 21 组）、`server/index.html`、`第四周-用自然语言查询与请求采集.md` §十四、`第五周-传感器示波器与姿态孪生.md` §十一 |
| 第 5 周 | 逐屏对账参考实现 + 最小可行范围 + `做，做完就上传github` | 见下方提交历史 | `firmware/main/wave.c`、`wave.h`、`qma7981.c`（I2C 互斥）、`main.c`、`app_config.h`、`server/server.py`、`server/selftest_wave.py`、`server/selftest_page.js`、`server/index.html`、`第五周-传感器示波器与姿态孪生.md` |
| 第 2 周欠账补做 | `请继续执行任务`（AI 翻计划书原文，补「不是可选项」的断网缓冲） | `22e0566` | `firmware/main/backlog.c`、`backlog.h`（新）、`main.c`、`app_config.h`、`partitions.csv`、`CMakeLists.txt`、`server/server.py`、`server/nl_agent.py`、`server/selftest_backlog.py`（新）、`server/index.html`、`断网补传设计说明.md`（新） |

**完整提交历史**（`git log --oneline --reverse` 可复核；笔数用
`git log --oneline | wc -l` 现查，不在这里写死 —— 写死了每次补记都会过期）：

> 下面列到断网补传补做（`22e0566`）为止。之后的文档类提交未逐笔列出，
> 因为它们不改功能，只改文字。

```
fc48008 初始版本：ESP32-S3-EYE 加速度计采集 + Wi-Fi 上传（不含摄像头）
5b2738d feat: 新增 OV2640 摄像头采集 + 无线实时传图
0af55de docs: 更新部署路径与仓库信息，补充两条避坑说明
93cd050 docs: 新增仓库根 README，并修正 ai-week1/README 的结构说明
53a7bb9 docs: 补齐摄像头功能的部署说明、接口清单与排错条目
8b836a8 fix: 修正不存在的 SCCB 配置项，改为真正生效的 CONFIG_SCCB_* 端口 0
7cf0848 docs: 摄像头文档的提交历史更新到当前 6 个提交
6c77b0b fix: 关闭 Wi-Fi 省电、保留上传错误码；新增项目复盘与踩坑记录
1f442d9 docs: 复盘报告补充「credential.helper 被包一层导致取不到凭据」的排查
4d72ab7 docs: 修正复盘报告中的章节交叉引用（5.4 -> 5.5）
266642e chore: 仓库结构调整，作业统一到 ai_week/ 并去掉周次分层
fcfa37f feat(server): 新增远程采集指令通道与请求状态机
e970d75 refactor(server): 设备轮询接口只回执行所需字段，自测脚本改为每次换设备号
b8b3a7d feat(web): 新增远程采集按钮、请求状态时间线与按 request_id 取图
8762f95 feat(firmware): 新增命令轮询通道与 boot_id/seq 新鲜度标识
bee01d4 docs: README 补第2周接口一览、课堂验证步骤与排错条目
27ba443 fix: 加固高频短连接下的连接回收（排障记录）
20760cd fix(server): 回补 _send_json 漏写的响应体，并让自测脚本绕开本机代理
289d679 feat(server): 照片画廊与数据配额——元数据与哈希永久保留，原图按天清理
d05e32f feat(web): 新增照片画廊区块；docs 补画廊/配额说明与两条排错经验
f508735 docs: 新增《AI 协作记录与提示词归档》，满足考核对"对话与主要提示词"的要求
a1dc9bf docs: 新增外壳/结构/载板 PCB 设计方案与三张 2D 图纸
e532373 docs: 复盘补记 git push 卡死的真正根因与可用命令
6fdf5ce feat(firmware): 第3周按键求助通道 —— BOOT键发起 + LED本地反馈
3ce8956 feat(server): 第3周求助事件通道 —— 三层状态分列存储 + 网页三张状态卡
0724a96 feat(server): 第4周自然语言查询与请求采集 —— 受限工具 + 防"假成功"三层防线
14aefa6 docs: 第3/4周设计文档 + README 同步 + 复盘与 AI 协作记录补记
2178944 fix(firmware): 板端逐行审计抓出 4 个问题（编译通过 ≠ 行为正确）
f9d8664 feat(test): 同伴走查引导脚本 + 第4周「无响应记录」+ 文档同步
14d5a33 docs: 参考实现演示视频逐屏对标 + 更正一处"做不了"的范围
35ff65a feat: 第5周传感器示波器与姿态孪生 —— 20Hz 批量上传 + 时间轴三档 + CSS 3D 孪生
9d5938e feat(nl): 第5周波形/姿态数据接入自然语言问答 —— 补白名单 + 2 只读工具 + 航向守卫
22e0566 feat: 断网补传 —— 离线 Flash 缓存与恢复上传（第2周计划书欠账补做）
```

---

## 六、可复现性声明

这份归档不是「事后写的好听话」，每一条都能当场复核：

| 想验证什么 | 怎么做 |
|---|---|
| 提交记录是否完整、是否与本文档一致 | `git log --oneline --reverse` |
| 第 2 周指令通道状态机是否真的能跑 | `python server/selftest_command.py`（25 项断言，连已运行的服务端） |
| 照片画廊与配额清理是否真的生效 | `python server/selftest_gallery.py`（20 项断言，自起隔离实例） |
| 「旧值冒充」是否真的被拦住 | 跑 `selftest_command.py` 的场景 4（E2 时序）与场景 5（E3 序号） |
| 波形往返是否真的逐点一致、姿态四档是否判得对 | `python server/selftest_wave.py`（11 组主断言 + 3b/3c/7b，共 14 段，自起隔离实例） |
| 姿态孪生的**几何**是否正确 | `node server/selftest_page.js` 第 6 组（断言渲染出的 `transform`，不是中间变量） |
| 时间轴三档是否会正确降级 | `selftest_wave.py` 第 7b 组（`device` / `derived` / `server` + 两种降级条件） |
| 第 5 周数据是否真的接进了问答层 | `selftest_nl.py` 第 18 组（只上传波形的设备算不算已知设备）、第 19~20 组（姿态边界 + 波形摘要） |
| 「航向不可测」有没有守住、会不会误伤 | `selftest_nl.py` 第 21 组（4 条拦 + 3 条不误伤）；也可在网页问「板子朝哪个方向」看模型自己怎么答 |
| 补传帧会不会冒充"刚拍的" | `python server/selftest_backlog.py`（9 组 72 条断言，自起隔离实例）第 1~3 组 |
| 补传帧带 `request_id` 是否真的被硬拒 | `selftest_backlog.py` 第 2 组（含"被拒的帧没有落盘、没有入库"两条） |
| 队列顺序不变量有没有破 | `selftest_backlog.py` 第 7 组（**读 `backlog.c` 的格式串逐字符对账** + 一条不补零的反例） |
| 淘汰计数是不是诚实的 | `selftest_backlog.py` 第 8 组（丢最老 / 如实计数 / **先传成功再删**） |
| 板子数据是否真的来自本组设备 | 按 `README.md` 第四章「检查项 1~3」现场操作 |
| AI 在排障时用的原始证据 | `项目复盘与踩坑记录.md`、`摄像头功能方案与Git提交.md` |

**一句话总结这份文档的立场**：

> AI 在这门课里不是「代写作业的工具」，而是**一个需要被反复核对的合作者**。
> 它的价值在于能立刻写出实现、能翻遍日志找出根因、能把踩过的坑沉淀成文档；
> 它的风险在于会自信地给出错误结论、会顺手改坏能跑的代码、会顺着你的截图假装功能做完了。
> **所以人必须始终拿着那把尺子：这个结论，证据在哪？**

